# bulk downloads, brought back by fetch.py
raw/

# per-family tables, built from raw/ by fetch.py
taxa/
index.tsv
manifest.tsv

# pilot fits and their drivers, made by pilot/run.sh. output.40M/ holds the logs a rerun
# displaced, so it is output too.
pilot/output*/
pilot/aux*/
pilot/failures.log
pilot/rerun.out
pilot/rerun.pgid
